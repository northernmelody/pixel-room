# 竖版功能覆盖清单

状态：2026-09-17。核心自主运行、边界和交互已通过静态契约与真实浏览器验证；“已实现”与“已通过”仍分开记录，未完成的视觉等价项列在文末。

本轮时间线改造（2026-09-17）：`life.js` 现在按上海/北京时间的日期生成确定性的星期档案。工作日共享晨间、午间和工作骨架，晚间按周一至周日分别进入恢复、运动、兴趣、轻休闲、周末前、自由日和收束日档案；换衣只出现在晨间，睡前包含 `undress` 语义，秋千可在早餐后的晨间缓冲段发生。时间线不再从单一晚间随机池抽取活动。

## 交互原则

- 人物、猫和狗按北京时间及各自状态机自主活动。
- 正式页面没有移动、楼层切换或“开始任务”按钮，点击空地不改变任何角色目标。
- 点击人物、猫、狗只增加短时回应层，不停止移动、不重置活动计时、不改变目的地。
- 原横版由点击触发的跟狗、连续摸猫走开、弹吉他和换衣，改为自主行为；其内容和动画仍保留在活动池。

## 横版到竖版的覆盖

| 范围 | 竖版实现 | 主要代码 |
| --- | --- | --- |
| 北京时间与测试时间 | 真实 UTC+8；保留 `?t=HH:MM:SS` 开发入口 | `js/time.js` |
| 每日作息 | 睡眠、晨间洗漱、三餐、工作、晚间休闲、通话、淋浴 | `js/life.js` |
| 夜间活动 | 按日期确定是否发生如厕、喝水、夜宵、手机翻身 | `js/life.js` |
| 三餐 | 38 道菜按日期选择；冰箱、烹饪、端饭、吃饭 | `js/life.js`、`js/scene.js` |
| 工作 | 电脑工作、短暂喝水、回到工作区 | `js/life.js` |
| 休闲 | 游戏、阅读、三类运动动画、逗猫、看窗、手机、6 种零食、三套换衣、3 首弹唱、秋千 | `js/life.js` |
| MOMO | 100 天通话主题及完整记录、两封信、聊天电脑画面 | `js/content/index.js`、`js/main.js` |
| 信件上传 | 设置页两套本地文件槽；TXT、Markdown、JSON；本地存储并覆盖阅读内容 | `../setting/`、`js/letters.js` |
| 电脑 | coding、video、chat、slacking、art、game 六频道及换台 | `js/main.js` |
| 七夕内容 | 2026-08-19 聊天屏粉色主题、爱心和专属标题 | `js/main.js` |
| 吉他 | 自动取琴、携带、床边弹唱、逐句歌词、归还；三首歌轮换 | `js/life.js`、`js/main.js`、`js/audio.js` |
| 猫 | 发呆、漫游、理毛、睡眠、狂奔、蹭人、攀爬／高处、抓窗帘、床下、吃粮、逗猫联动 | `js/pets.js` |
| 狗 | 发呆、漫游、睡眠、吃粮、狂奔、自主跟随、吠叫、抓挠、坐下 | `js/pets.js` |
| 角色点击 | 独立短时回应；不修改自主状态 | `js/life.js`、`js/pets.js` |
| 四季和昼夜 | 默认按北京时间自动，也可在设置中预览覆盖 | `js/store.js`、`js/scene.js` |
| 天气 | Open-Meteo 北京天气、7 秒超时、本地兜底、雨雪雷粒子 | `js/environment.js`、`js/scene.js` |
| 灯光 | 六盏灯；默认按时间自动，设置页可转为手动控制 | `js/store.js`、`js/scene.js`、`../setting/` |
| 动态家居 | 被子、咖啡杯、粮碗、冰箱、锅和蒸汽、餐盘、衣柜、水流、季节风扇／加湿器 | `js/scene.js` |
| 声音 | 默认静音；音量、界面反馈、猫狗、键盘、吉他及简化环境提示音 | `js/audio.js` |
| 快递与收藏 | 跨日补粮、快递概率、1–3 日拆箱、五类收藏去重展示 | `js/store.js` |
| 存档 | 设置、世界进度、角色位置、上传信件使用独立竖版键 | `js/store.js`、`js/letters.js` |
| 原家具与内容 | 53 个原绘制对象、4 个房间、4 扇窗、6 盏灯、7 个玩偶、38 道菜、6 种零食、3 首歌、100 组通话、2 封信 | `js/layout.js`、`js/art/`、`js/content/` |

## 新增庭院秋千

秋千位于前院左侧，使用木质 A 字支架、双绳和单人木座板。人物在休闲时自主打开院门、走到秋千、摆动并在活动结束后继续下一项生活。21:00 后及雨、雪、雷雨天气不新安排秋千活动。秋千本身可点击查看说明，不会让人物立即使用。

## 验证状态

### 通过

- 北京时间作息边界、三餐完成与不重复、21:30 通话、22:00 淋浴、22:30 睡眠。
- 人物、猫、狗在移动和动作中的点击反馈不改变活动、目的地、动作步进或计时；高处猫热点与空地误命中已修复。
- 两级楼梯与庭院路径连续移动、秋千路线和返回；自动/手动灯光、默认静音、音量 0/100、天气成功与失败兜底。
- 100 天通话、38 道餐食、6 种零食、3 首歌词、53 个绘制对象、4 个房间、4 扇窗、6 盏灯、7 个玩偶、两封信解析和独立竖版存储。
- 真实浏览器多尺寸布局和页面日志；证据见 `artifacts/s6-browser-results.txt`。

### 修复后通过

- 早餐原计时长于作息窗口、导致餐食永不完成；已改为完成后进入休息并防止重复记账。
- 高处猫点击仍使用地面坐标；已改为按显示高度命中。
- 自动灯切手动时设置页显示旧值；已改为基于有效灯状态切换。
- UTF-8 文件大小、文件扩展名和 JSON 信件结构校验；天气超时清理和无效响应兜底。
- 新画布 216×450 与庭院扩展后的 320px 横向溢出；已收紧响应式宽度。

### 尚未验证或已知不等价

- 电脑 video/game 仍使用简化动画矩形，chat 未接入横版原背景；运动、猫狗部分姿态和高处/床下/抓挠等仍是简化表现。
- 音频已验证静音、启用和提示音链路，未逐项听感验收冲水、键盘、猫狗和完整吉他编曲。
- 未做真实断网浏览器演练、跨多日后台恢复、七夕日期的完整视觉回归，以及长时间数小时稳定性测试。
- 原版 smoke 中 `smoke-guitar.js`、`smoke-layout.js`、`smoke-regressions.js` 仍是旧横版坐标/接口断言，记录为 legacy failure，不代表竖版 S6 契约失败。

## 独立验证任务重点

1. 用测试时间覆盖所有作息边界、夜间事件、三餐和 21:30 通话。
2. 验证每种动作的站位、遮挡、持物和收尾，尤其是楼梯、衣柜、淋浴、吉他与秋千。
3. 在人物、猫、狗移动和动作中连续点击，确认自主状态、目标和计时没有被改写。
4. 检查猫狗跨层、高处、床下、粮碗消耗以及刷新后的世界进度。
5. 检查 6 个电脑频道、100 天通话索引、上传信件、三首歌词及七夕指定日期。
6. 检查天气 API 成功／失败、自动灯光、声音开关、跨日快递与收藏。
7. 检查桌面及 320×568、375×812、390×844、430×932，修复控制区撤下后的布局和热点问题。
8. 更新自动契约测试；旧 S3/S4.1 针对手动控制的断言应替换为自主行为断言。
