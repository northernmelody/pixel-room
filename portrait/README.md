# Pixel Room 竖版：S6 主体开发记录

猫叫和狗叫使用 `assets/audio/` 中的真实 WAV 录音；授权与来源见 [`assets/audio/SOURCES.md`](assets/audio/SOURCES.md)。浏览器无法加载或解码时，`js/audio.js` 会降级到原有合成音，避免音频阻塞主体验。

日期：2026-09-17。横版保护基线：`7a78c0f`。

竖版已经从手动移动演示切换为自主生活场景。人物按北京时间执行作息，猫狗分别活动；点击角色不会停止或改变其作息。横版的内容、组件和主要系统均已在竖版建立运行路径，并新增庭院秋千以及设置页两封信上传。

本轮完成了点击范围收束和横版交互迁移：只有横版本来就有的入口可点（人物、猫、狗、吉他、电脑、六盏灯、玩偶与抱枕、两封信、餐食热点、衣柜、户外门、通话卡片），装饰家具、窗户、地毯、摆件和秋千不再弹窗也不再显示悬浮。当前结论是“核心自主运行与交互验收通过”，仍不把它称为横版功能的完全等价验收；简化动画和真实离线/多日场景见 [功能覆盖清单](COVERAGE.md)。

## 入口

在仓库根目录运行：

```powershell
python portrait/tools/serve.py
```

- 竖版（主入口）：`http://127.0.0.1:8138/portrait/`
- 设置：`http://127.0.0.1:8138/setting/`
- 横版：`http://127.0.0.1:8138/classic/`

生产入口已切换为竖版：根路径跳转到 `/portrait/`，横版退到 `/classic/`，部署在 Vercel（`https://pixel-room-eight.vercel.app`）。本地用 `?d=YYYY-MM-DD&t=HH:MM:SS` 可以固定日期与时刻复现任意作息与星期档案。

## 本轮验证结果

- `npm --prefix portrait test`：通过；包含 S6 自主作息、10 个边界、点击范围白名单、猫摸链与狗跟随、手动吉他与衣柜规则、通话当前句、庭院路径、三首歌词、100 天通话、信件解析、天气、音频和存储隔离契约。
- 原版保护检查：通过，23 个横版文件哈希未变化。
- 真实浏览器点击回归：`node portrait/tools/browser-check.cjs` → `artifacts/browser-check.log`，ALL PASS，页面异常 0；截图 `_shots/portrait-browser-check.png`、`_shots/portrait-call-card.png`。覆盖 4 个作息边界、两类角色点击、装饰物件无命中、玩偶/餐食/木门提示、电脑与两封信、吉他弹唱与晚间换衣、闪灯吓猫、通话卡片。
- 真实视口：320×568、375×812、390×844、430×932、1440×1000 无横向溢出；设置页 320px 控件也无横向溢出。
- 设置页与内容：两封本地 Markdown/JSON 信件上传、刷新恢复、恢复内置信件、六频道电脑、12 句通话、声音开关、自动/手动灯光和音量端点均已检查。
- 早期证据 `artifacts/s6-browser-results.txt` 早于星期档案改造，其中的固定作息时刻（21:29 通话等）已过时，仅作历史参考。

## 正式交互

页面打开后自动按北京时间进入当前生活阶段，没有移动按钮或任务启动按钮；点击空地不控制人物。点击范围与横版一致，`js/layout.js` 的物件 `action` 字段是唯一白名单。

- 人物：短时回应气泡与提示音，不改作息、路线和计时。
- 猫：摸猫链——3 秒内连点到第 4 次会伸爪走开；高处先落地、床下先出来。
- 狗：首次点击吠叫，4 秒内再点跟随 10 秒。
- 吉他：点击取琴弹唱并逐句显示歌词；正在弹唱、睡着或通话时给出文字提示。
- 电脑：点击书桌打开六频道放大画面，可换台、点遮罩或关闭按钮退出。
- 六盏灯：点击开关，也可以在设置页控制；5 秒内连闪 3 次会把猫吓进床底（30 秒冷却）。
- 玩偶与腊肠狗抱枕：点击显示名称提示。书架：两封信的正文。吊柜／台面／餐桌：当前食物，没做饭时提示餐桌是空的。衣柜：晚间休闲时换衣，其他时间提示稍后再来。户外木门：显示未开放提示。
- 通话：晚间通话时段左下角出现 MOMO 卡片，点击阅读完整记录。
- 装饰家具、窗户、地毯、摆件、家电、粮碗、快递箱与秋千：不可点击、无悬浮标签、不弹窗。
- 声音默认关闭，用户主动开启后才创建 WebAudio 上下文。

## 自主生活

人物包含睡眠、晨间如厕／冲水／洗手／刷牙、三餐、工作与喝水、晚间休闲、晚间通话、淋浴、夜间如厕／喝水／夜宵／手机活动。晚间休闲包括游戏、阅读、运动、逗猫、看窗、手机、零食、换衣、完整弹唱和庭院秋千；具体顺序由当天星期档案决定。

猫包含漫游、理毛、睡眠、狂奔、蹭人、攀爬与高处停留、抓窗帘、床下、进食和逗猫联动。狗包含漫游、睡眠、进食、狂奔、自主跟随、吠叫、抓挠和坐下。两只宠物使用与人物相同的楼梯导航图，但各自拥有状态和速度。

庭院新增木质 A 字秋千。天气和时段合适时，人物会自己经过院门走到秋千；秋千本身是不可点击的场景道具。

## 内容与环境

- 保留 53 个原绘制对象、4 个房间、4 扇窗、6 盏灯、7 个玩偶。
- 保留 38 道餐食、6 种零食、100 天 MOMO 通话、2 封信和 3 首原创歌曲。
- 电脑覆盖 coding、video、chat、slacking、art、game 六种画面，保留固定日期的七夕主题。
- 默认根据北京时间决定昼夜和四季；北京天气来自 Open-Meteo，失败时使用本地兜底。
- 支持雨雪雷天气粒子、季节设备动态、自动灯光、音量、星空和动画细节设置。
- 跨日补充宠物粮，推进快递和五类收藏；竖版世界与横版存档隔离。

## 信件上传

`/setting` 为两封信各提供一个文件槽，支持 TXT、Markdown 和 JSON。文件在浏览器本地读取并存入 `pixel-room-portrait-letters-v1`，不会发送至服务器。Markdown 首行 `# 标题` 可以覆盖信件标题；JSON 支持 `title`、`from`、`to`、`paragraphs` 或 `body`。每个文件限制 100 KB，可以一键恢复内置信件。

## 主要模块

| 模块 | 职责 |
| --- | --- |
| `js/time.js` | 北京时间、`?t=` 时刻与 `?d=` 日期测试入口、日期编号 |
| `js/life.js` | 星期档案、自主作息、动作序列、休闲、夜间、餐食、歌曲、手动弹唱/换衣入口与回应 |
| `js/pets.js` | 猫狗自主状态、跨层导航、进食、摸猫链、狗叫/跟随与受惊躲藏 |
| `js/environment.js` | 北京天气请求、超时和离线兜底 |
| `js/audio.js` | 用户启用后的 WebAudio 反馈、动作和环境提示音 |
| `js/store.js` | 设置、动态物品、快递收藏、跨日和存档隔离 |
| `js/letters.js` | 两封信上传解析、覆盖和恢复 |
| `js/navigation.js` | 三层楼梯、院门与庭院路径 |
| `js/layout.js` | 物件、房间锚点、六盏灯与可操作白名单（`action`） |
| `js/scene.js` | 房屋、家具、动态角色、秋千、动作道具、天气、光照与点击命中 |
| `js/main.js` | 页面装配、点击分派、内容弹窗、电脑、通话卡片和主循环 |

## 已执行检查

```powershell
npm --prefix portrait run check
npm --prefix portrait test
python portrait/tools/serve.py          # 另开一个终端
node portrait/tools/browser-check.cjs   # 真实 Chromium 点击回归
```

`check` 执行模块语法检查；`test` 还会运行自主行为与点击范围契约、以及原版保护检查。`browser-check.cjs` 通过 CDP 驱动本机 Chrome，把 `tests/browser.html?autorun` 的真实点击结果写入 `artifacts/browser-check.log` 并截图；它需要 Chrome 与 `serve.py` 同时可用。原版受保护文件没有在本轮修改。旧版横向 smoke 中仍有三项坐标/接口断言失败，详见 [功能覆盖清单](COVERAGE.md)，不计入竖版契约结果。
